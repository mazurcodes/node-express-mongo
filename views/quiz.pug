extends layout

block content
  h1= title
  form(method="post")
    p Jaka jest twoja ulubiona pora roku?
    ul
      each item in data
        li
          if show
            label=item.title
            input(type="radio" name='quiz' value=item._id)
          else 
            p #{item.title} - liczba głosów: #{item.vote} - głosów ogółem: #{Math.round(item.vote/votesTotal * 100)}%
    if show
      input(type='submit' value='Zagłosuj')
      p Aby zobaczyć wyniki oddaj głos
    else 
      h3(style="color: red") Dokonałeś już głosowania w ankiecie. Dziękujemy :)